# KLAdaptiveSurface

`KLAdaptiveSurface` turns an image-derived color into a readable surface while retaining the source hue when possible. It does not load images, own view state, or decide how a host view is rendered.

Use it when a sampled color can become muddy, too dark, or too saturated behind text and controls.
